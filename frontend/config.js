// WorkBridge ETH — Frontend configuration
//
// This file is loaded by index.html BEFORE js/api.js (see the <script> tag in
// the <head>). Edit the value below to point at your deployed backend, then
// redeploy.
//
// Examples:
//   window.WB_API_BASE = 'https://api.workbridge-eth.com/api';
//   window.WB_API_BASE = '/api';  // when served from the same domain via a reverse proxy

window.WB_API_BASE = 'https://workbridge-eth-backend.onrender.com/api';